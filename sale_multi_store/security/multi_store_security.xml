<?xml version="1.0"?>
<odoo noupdate="0">
    <!-- ESI correccion 2026:
         - Sin sucursal: visible para todos (borradores sin Tipo de Venta).
         - Con sucursal: solo la sucursal activa del usuario y sus hijas.
         - Si el usuario no tiene una sucursal activa, se usan sus sucursales
           permitidas como respaldo.
    -->
    <record id="sale_order_store_rule" model="ir.rule">
        <field name="name">Ventas por sucursal</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="global" eval="True"/>
        <field name="domain_force">['|', ('store_id', '=', False), ('store_id', 'child_of', ([user.store_id.id] if user.store_id else user.store_ids.ids))]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <record id="sale_order_store_visibility_esi_rule" model="ir.rule">
        <field name="name">Ventas por sucursal ESI</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="global" eval="True"/>
        <field name="domain_force">['|', ('store_id', '=', False), ('store_id', 'child_of', ([user.store_id.id] if user.store_id else user.store_ids.ids))]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>
